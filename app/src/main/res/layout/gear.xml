<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/popup_gear_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#AA000000"> <!-- Dimmed background -->

    <!-- POPUP CARD -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="20dp"
        android:orientation="vertical"
        android:background="#222222"
        android:padding="12dp"
        android:elevation="8dp">

        <!-- HEADER -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#1E3A8A"
            android:padding="12dp"
            android:gravity="center">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="GEAR"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="@android:color/white" />
        </LinearLayout>

        <!-- MAIN CONTENT -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:paddingTop="12dp"
            android:paddingBottom="12dp">

            <!-- Avatar image -->
            <ImageView
                android:id="@+id/avatar_image"
                android:layout_width="120dp"
                android:layout_height="200dp"
                android:layout_alignParentEnd="true"
                android:layout_marginEnd="12dp"
                android:src="@drawable/male2"
                android:scaleType="fitCenter"
                android:background="@android:color/darker_gray" />

            <!-- Lock grid -->
            <GridLayout
                android:id="@+id/grid_locks"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_toStartOf="@id/avatar_image"
                android:layout_alignTop="@id/avatar_image"
                android:layout_alignBottom="@id/avatar_image"
                android:layout_marginEnd="12dp"
                android:layout_marginStart="12dp"
                android:columnCount="4"
                android:rowCount="4"
                android:alignmentMode="alignMargins"
                android:useDefaultMargins="true">

                <!-- 12 Lock icons -->
                <!-- Copy these as needed -->
                <ImageView
                    android:layout_width="50dp"
                    android:layout_height="50dp"
                    android:background="@android:color/darker_gray"
                    android:src="@android:drawable/ic_lock_lock" />
                <!-- Repeat above line 11 more times -->
            </GridLayout>

            <!-- Save Button -->
            <Button
                android:id="@+id/btn_save"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignEnd="@id/avatar_image"
                android:layout_below="@id/avatar_image"
                android:layout_marginTop="8dp"
                android:text="SAVE"
                android:backgroundTint="@android:color/holo_green_light" />
        </RelativeLayout>

        <!-- Bottom Buttons -->
        <LinearLayout
            android:id="@+id/bottom_buttons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:padding="4dp"
            android:background="#1E3A8A">

            <Button
                android:id="@+id/btn_weapon"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="WEAPON"
                android:backgroundTint="#FFD700" />

            <Button
                android:id="@+id/btn_armor"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="ARMOR"
                android:backgroundTint="#FFD700" />

            <Button
                android:id="@+id/btn_pants"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="PANTS"
                android:backgroundTint="#FFD700" />

            <Button
                android:id="@+id/btn_boots"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="BOOTS"
                android:backgroundTint="#FFD700" />

            <Button
                android:id="@+id/btn_accessories"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="ACCESSORIES"
                android:backgroundTint="#FFD700" />
        </LinearLayout>

        <!-- CLOSE BUTTON -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#444444"
            android:gravity="end"
            android:padding="8dp">

            <Button
                android:id="@+id/close_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="CLOSE"
                android:textColor="#FFFFFF"
                android:backgroundTint="#EF4444" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
