package com.example.fitquest;

public enum QuestCompletionType {
    SINGLE,      // Complete in one session (daily quests)
    ACCUMULATED  // Complete over multiple sessions (weekly/monthly quests)
}
